# infrastructure-dh

Coloca aquí tu código Terraform.

El provider ya está configurado para apuntar a LocalStack o AWS real según `TARGET_ENV`.
Consulta `IDEA.md` para ver el bloque `provider "aws"` con los endpoints condicionales.
